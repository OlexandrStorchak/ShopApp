<div class="row g-3 mt-3 m-4">
  <%@categories.each do |cat|%>
  <div class="card">
    <%=cat.name%><%=link_to "Show",product_category_path(cat)%><%=link_to "Edit",edit_product_category_path(cat)%>
    </div>
  <%end%>
</div>
